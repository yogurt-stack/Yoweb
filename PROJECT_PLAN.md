# 个人 AI／软件工程岗位面试题库与训练系统

## 1. 文档说明

- 文档状态：架构与项目计划已确认，待进入实现阶段
- 产品形态：单人使用的 Web 应用
- 主要使用者：项目所有者本人
- 核心优先级：个人题库与训练 > 面经聚合 > 岗位市场分析
- 当前阶段：需求定义、数据语义、查询需求和技术选型已完成
- 实现原则：正确性 > 可维护性 > 成本 > 简单性 > 炫技

本计划书记录此前 D0～D17 的确认结果，并作为后续开发、评审和验收的共同依据。模型价格、网站访问规则和第三方服务条款属于易变信息，真正接入前必须重新核实。

---

## 2. 项目定位

本项目不是通用面试平台，也不是面经聚合网站，而是一个服务个人求职的：

> 以 Question 为核心、以来源证据为约束、以受控 Workflow 为主、以 LLM 为整理和训练助手的个人面试题库系统。

系统要帮助用户：

1. 获取高质量的真实面试题和岗位相关题目。
2. 直接创建、整理、分类和去重个人题目。
3. 追溯题目的来源、公司、岗位、时间和面试上下文。
4. 根据目标岗位和薄弱领域筛选题目。
5. 进行基础 AI 面试训练并记录历史表现。
6. 持续从精选公开来源发现新的高价值题目。

岗位市场分析属于辅助能力，首版不追求严格的行业统计结论。

## 3. 非目标

首版不包括：

- 商业化和多用户权限体系
- 大规模全平台爬虫
- 未经授权的登录限制、验证码或反爬绕过
- 复杂推荐系统
- 自主联网搜索 Agent
- Multi-Agent 协作
- 云端集群、微服务、Kafka、Kubernetes、Temporal
- 手机 App、小程序、浏览器插件、桌面客户端
- 复杂的长期能力画像和自适应教学模型
- 代表整个行业的严格统计分析

---

## 4. 已确认的决策记录

> 下列内容保留讨论中使用的主要 D 编号。部分早期零散子决策按主题合并记录，语义与已确认内容一致。

### D0：产品目标与优先级

**Decision**

优先建立服务个人求职的高质量训练题库；岗位市场分析作为辅助能力。

**Reason**

题目的相关性、来源可信度和训练价值，比收录数量更重要。

**Alternatives**

- 通用面试平台
- 以市场分析为中心
- 大规模内容聚合平台

**Trade-offs**

接受覆盖有限、部分人工整理和统计不代表整个行业。

**Impact**

数据源采用小范围筛选；保留出处；暂不引入多用户、大规模采集和复杂推荐。

### D1：数据源策略

**Decision**

采用“精选来源＋人工/自动化混合获取”。

**核心来源**

- 牛客：真实面经、面试题线索和面试上下文。初期人工发现与辅助整理，不建立未经授权的批量爬取机制。
- 精选 GitHub 仓库：面试题汇编、Agent/LLM/RAG、后端和算法资料，作为第一批自动化候选；必须检查 License、内容来源和 GitHub API 条件。
- 目标公司招聘官网：JD、岗位职责和技术要求，优先关注实际准备投递的公司，逐站判断自动化条件。
- LeetCode：保存题号、标题、URL、标签、难度和个人练习状态，不以批量复制题目正文为目标。
- 其他博客、知乎、掘金、CSDN 等社区：人工补充，暂不全平台自动接入。

**Reason**

高质量个人题库比大量低可信内容更重要。

**Alternatives**

- 全人工
- 只自动化可访问来源
- 全平台自动采集

**Trade-offs**

接受人工整理和有限覆盖，换取质量、合规性和长期可维护性。

**Impact**

所有题目和证据需要保留 `source`、`source_url`、`source_type`、采集时间、证据范围和内容来源属性。练习题、面经原题、JD 要求和模型生成题不得混为一类。

### D2：题目优先的数据语义

**Decision**

Question 可以先于来源、面经、岗位和公司进入系统。来源证据尽量随附，但不是题目录入前置条件。

**Reason**

个人题库的第一目标是低成本收录、整理和训练题目。

**Alternatives**

- 来源优先
- 面经优先
- 完整 Raw Document 优先

**Trade-offs**

部分早期题目只有训练价值，暂时不能参与公司、岗位或时间维度统计。

**Impact**

Question 是核心对象；Source、Raw Document、Interview、Job 和 Occurrence 都是可选上下文或证据对象。

### D2.2：Question 的定义

一道 Question 是一个可以被独立作答和评价的任务。判断是否为同一道题，主要看：

- 核心考查目标是否相同
- 要求完成的作答任务是否相同
- 关键约束是否兼容
- 合并后是否会损失重要语义

因此：

- “解释 Agent Loop”和“设计一个 Agent Loop”默认是不同题目，因为作答任务不同。
- 一道大题默认完整保留；只有子题能独立作答和评价时，才拆成子题，并保留与大题的关系。
- 知识领域与作答任务分开表示。
- 分类、标签和难度是可修订的采用结果，不是不可变事实。
- 来源中的难度与题库参考难度分开。
- 第一阶段整理状态为“待整理”和“已整理”；个人掌握程度通过训练记录表达。
- 用户自己提出的题目属于自建 Question，可以没有来源。
- 自建 Question 的“加入方式”与来源内容的“内容来源”分开记录。

### D2.2-c：原始问法与标准 Question

**Decision**

- 原始问法与标准 Question 分离。
- 一个标准 Question 可以关联多个符合“同题标准”的原始问法。
- 原始问法始终保留，不被标准题覆盖。
- 无法确定是否同题时，不强制归并。
- 自建 Question 可以没有原始问法。
- 从原始问法扩展出的训练要求，不得反向认定为实际面试原题。

### D2.2-d：题目关系与频率

必须区分两类频率：

- 资料收录频率：题目出现在多少份公开资料中。
- 面试报告频率：有多少段可区分的面试经历报告问过该题。

Occurrence 只表达真实面试经历中的报告出现。GitHub 汇编、JD、LeetCode 练习和模型生成内容都不自动产生 Interview Occurrence。

### D2.2-e：训练记录与题目生命周期

Question 与训练尝试分离。一道题可以有多次回答历史，训练记录不能覆盖题目本身。

第一阶段只保留简单整理生命周期：

```text
待整理 → 已整理
```

未来的“已验证”“已掌握”等状态应通过来源验证和训练表现推导或另行设计，不提前把复杂掌握度写入题目状态。

### D3：来源材料与 Raw Document

Raw Document 是可选的来源材料，可代表面经、GitHub 文件、JD 或博客。Question 不要求拥有完整 Raw Document。

Source 表示来源平台或站点；Evidence 表示题目对应的可追溯证据；Interview 仅在材料描述一次面试经历且上下文有价值时建立。

### D4：来源内容保存范围

按来源权限分级保存：

- 自建内容或明确允许保存的内容：可保存全文。
- 开放许可 GitHub 内容：遵守许可证、署名和来源条件。
- 牛客、LeetCode、招聘网站和一般社区：默认保存链接、元数据、必要证据片段和个人笔记，不批量归档全文。
- 题目尽量保留可追溯证据。
- “允许本地保存”与“允许发送外部模型”分开判断。
- 接入具体来源前逐站核实条款、robots 和公开接口。

### D12.1：Raw Document 权限分级

这是 D4 的正式记录，作为实现时的内容策略依据。

### D12.2：外部 LLM 数据外发

**Decision**

采用“受控、最小必要外发”：

- 默认只发送完成当前任务所需的内容。
- 必要且允许时，可以发送一篇完整相关面经，但不默认发送整个题库。
- 本地保存权限与外部模型发送权限分别判断。
- 不发送 Cookie、账号信息和无关个人信息。
- 记录调用目的、模型、时间和材料范围。
- 无法确认时改为人工整理或仅使用传统程序。

### D12.3：来源更新与版本策略

采用变化感知的版本策略：

- 重复检查同一页面不增加资料收录频率。
- 内容未变化时只记录最近检查状态。
- 内容有实质变化时保留允许保存的新旧证据或元数据。
- 来源更新只生成题目更新建议，不自动覆盖标准 Question。
- 页面失效后保留已有合法证据、个人笔记和失效状态。
- 区分“同一材料更新”和“新的独立材料”。

### D5：分类体系

采用“浅层知识目录＋补充标签”：

- 支持多领域归属，不强迫单一分类。
- 正式词表受控扩展，同义词通过别名管理。
- LLM 可以提出新分类或标签建议，但正式加入需要用户确认。
- 一级领域暂定：LLM、Agent、Coding Agent、RAG、后端工程、计算机基础、算法与数据结构。
- 系统设计暂用“知识领域＋方案设计”表达。
- 二级分类根据真实题目逐步建立，避免分类体系失控。

### D6：去重策略

去重分为两步：

1. 候选发现：字符串、别名、关键词和 Embedding 找到疑似重复。
2. 任务与约束判断：LLM 按同题标准判断。

LLM 输出受限关系：

- `SAME`
- `RELATED_BUT_DIFFERENT`
- `DIFFERENT`
- `UNCERTAIN`

用户最终确认。合并必须可恢复，并保留原内容、来源和训练历史。相似度只能表示“值得检查”，不能直接证明同题。

### D7：查询语义

- 基础关键词搜索和明确筛选必须独立可用。
- 自然语言查询是辅助入口。
- 岗位适配以人工确认关系为主，领域匹配只作为候选。
- 默认“高频”指资料收录次数；面试报告次数单独查询。
- 公司、岗位、时间查询采用严格主结果；证据不足的线索另列且不计数。
- 搜索标准 Question 和已确认关联的原始问法，最终以 Question 去重返回，并显示命中依据。

### D8：混合检索

**Decision**

采用：严格条件过滤＋关键词/别名＋Embedding 语义召回＋必要时 LLM 重排。

Embedding 负责相关候选，不负责证明公司、岗位、时间、面试频率或同题关系。

### D11.5：LLM 重排边界

默认由本地混合检索排序；普通关键词和明确筛选不调用生成式 LLM。只有 Chatbot/复杂歧义查询、疑似重复判断等场景，对少量候选使用 LLM 重排。

### D11.6：关键词与向量结果合并

- 严格条件先执行。
- 关键词与向量分别召回。
- 使用排名融合，不直接混合两种原始分数。
- 标题精确匹配和受控别名匹配获得确定性优先。
- 暂不写死关键词/向量权重。

### D11.7：确认关系持久化

已确认的同题、相关题和进阶题关系持久保存，查询时由程序稳定输出。检索和 LLM 只负责发现尚未确认的潜在关系，不在每次查询时重新推翻已确认关系。

### D11.8：候选数量与阈值

采用“Top K 上限＋最低候选阈值＋真实样本评估”：

- 相似度只决定候选发现，不决定同题。
- 查重和相关题搜索分别设置参数。
- 参数与 Embedding 模型版本关联。
- 更换模型后重新生成向量并重新评估。

### D11.9：检索评估

建立约 30～50 条起步的小型评估集，覆盖：

- 原样关键词
- 同义改写
- 模糊描述
- 分类筛选
- 完全无关查询

分别评估本地召回和 LLM Judge。日常失败案例持续加入，避免用 LLM 重排掩盖基础检索问题。

### D9：向量策略

- 向量保存于 SQLite。
- 首版由 Node.js 本地计算余弦相似度或模型规定的度量。
- 不引入 Qdrant。
- 首版精确遍历候选；性能不足时再评估 SQLite 向量扩展。
- Question 和已确认关联的 Original Wording 分别生成向量，最终归并到 Question。
- Raw Document 全文暂不进入题目语义索引。
- 每个向量记录 Provider、模型版本和维度。
- 更换模型或维度需要全量重新向量化。

### D10：LLM、Workflow 与 Agent 边界

首版以传统程序＋LLM Workflow 为主，不使用 Multi-Agent。

LLM 适合：

- 信息抽取
- 题目归一化建议
- 分类和难度建议
- 疑似重复判断
- 面经总结
- 回答评价
- 有限追问生成

传统程序负责：

- 状态流转
- 数据库查询和写入
- 来源证据统计
- 频率统计
- 权限策略
- 预算限制
- 最终合并确认
- 下一题决策

### D11：题库 Chatbot 边界

后续可以加入受控题库 Chatbot，允许用户用自然语言查题、组合筛选和解释推荐依据。模型输出受约束的操作和参数，程序校验后调用工具。

首版不允许 Chatbot 自主联网浏览、任意修改题库或读取整个数据库。开放式联网搜索 Agent 后置评估。

### D13：采集方式

采用“按来源适配、自动化逐级降级、浏览器仅作受控补充”：

```text
官方 API > RSS > 普通 HTTP > 浏览器自动化 > 人工处理
```

实际选择仍受来源规则约束。

| 来源 | 首选方式 | 自动化程度 |
|---|---|---|
| 用户自建题目 | Web 表单 | 人工 |
| 牛客 | URL、合法摘录、收录预览 | 人工/半自动 |
| GitHub | 精选仓库白名单、官方 API | 可自动 |
| 公司招聘官网 | 官方 API/RSS，逐站评估 HTTP 或浏览器 | 按站点 |
| LeetCode | 题号、链接和状态关联 | 人工/半自动 |
| 博客和社区 | URL、RSS 或人工摘录 | 以人工为主 |
| 搜索引擎 | 发现候选链接 | 不作为最终证据 |

禁止绕过登录、CAPTCHA、付费墙、访问限制和反爬机制。浏览器自动化是工具，不等于 Agent。自动获取内容先进入收录预览，不直接写入正式题库。

### D14：模型与 Provider

采用单供应商实际接入、生成式 LLM 与 Embedding 双接口抽象：

- 生成式模型与 Embedding 可以以后独立替换。
- 首版只真正接入一个供应商，避免多供应商路由复杂度。
- Qwen 固定版本作为默认评测候选。
- 具体模型必须通过固定样本评测后正式启用。
- 输出必须经过程序校验并进入收录预览。
- 模型 ID、价格、维度、版本和调用量可追踪。
- 月度约 15 元时提醒，20 元时暂停后台自动模型任务。

OpenAI、DeepSeek、Gemini 等作为后续评测或备选，不在 MVP 中自动路由。模型价格和数据政策在接入前重新核实。

### D15：个人训练 Workflow

采用“程序控制状态与选题、LLM 负责评价与有限追问”的受控自适应 Workflow，不采用自主训练 Agent。

训练过程：

```text
程序选择题目
→ 用户回答
→ LLM 按固定标准评价
→ 程序保存训练记录
→ 必要时有限追问
→ 程序进入下一题
```

程序根据目标岗位、领域、难度、历史表现和最近练习时间选题。复杂能力画像后置，首版保存每次回答、用户自评和 LLM 评价。训练生成的追问默认不成为真实面试题或 Occurrence。

### D16：本地运行与数据安全

采用“本机 Web 访问＋本地持久化＋自动版本化备份＋手动备份＋恢复验证”：

- 首版不开放公网。
- 自动备份保留多个历史版本。
- 定期验证备份是否可恢复。
- 后台任务失败可有限重试，不影响已有题库。
- 提供可迁移的数据导出。
- 异地备份和云端部署后置。

### D17：首版 MVP

首版采用“题目优先的完整 MVP”，覆盖：

- Question 快速创建、编辑和整理
- 原始问法、来源、公司、岗位和证据关联
- 精选 GitHub 导入与受限来源手动录入
- 收录预览和批量审核
- 去重候选、LLM 判断建议和用户确认
- 关键词、筛选和混合语义检索
- 按岗位和领域筛选
- 基础训练 Workflow
- 备份、恢复和导出

---

## 5. 总体架构

```text
Web UI
  ↓
应用服务层
  ├── Question 题库
  ├── Source / Raw Document 来源材料
  ├── Import Review 收录审核
  ├── Search 检索
  ├── Training 训练
  └── Settings / Backup 设置与备份
  ↓
SQLite
  ├── 结构化数据
  └── Embedding 向量
  ↓
后台 Worker
  ├── 来源更新
  ├── 抽取、归一化、分类
  ├── 去重候选
  ├── 向量生成
  └── 定时任务
  ↓
外部模型 Provider
  ├── 生成式 LLM
  └── Embedding API
```

采用模块化单体，不拆分微服务。Web 进程和后台 Worker 进程共享同一个 SQLite 数据文件，统一部署在本机。

## 6. 模块划分

### 6.1 Web 界面模块

- 题库首页和题目列表
- 快速添加题目
- Question 详情与编辑
- 收录 Inbox
- 搜索与筛选
- 来源和证据查看
- 训练页面与训练历史
- 后台任务、模型和备份设置

### 6.2 Question Domain 模块

负责题目创建、编辑、整理状态、分类、标签、难度、原始问法和题目关系。

### 6.3 Source/Ingestion 模块

负责来源登记、URL、获取方式、权限标记、Raw Document、版本变化和收录批次。

### 6.4 Processing Workflow 模块

负责规则提取、LLM 提取、归一化建议、分类建议、难度建议和去重候选。

### 6.5 Retrieval 模块

负责严格过滤、关键词/别名检索、Embedding 召回、排名融合和可选 LLM 重排。

### 6.6 Training 模块

负责训练会话、答题、评价、追问和历史记录。程序持久化状态，LLM 只输出评价和受限建议。

### 6.7 Worker/Scheduler 模块

负责定时检查、来源更新、批处理、失败重试、预算检查和任务日志。

### 6.8 Model Provider 模块

定义生成式 LLM 和 Embedding 的独立接口，记录 Provider、模型、版本、维度、Token 使用量和失败信息。

### 6.9 Backup/Export 模块

负责自动备份、手动备份、恢复验证和可迁移导出。

## 7. 数据模型说明

以下是概念 Schema，开发时再转换为具体数据库表和字段。

### 核心题库

- **Question**：标准化题目，是产品核心。
- **Original Wording**：原始问法，一个 Question 可有多个。
- **Question Relation**：相关、进阶、前置、变体、同题候选等关系。
- **Topic**：受控知识目录，支持层级。
- **Tag**：补充标签，支持别名。

### 来源与证据

- **Source**：来源平台或站点。
- **Raw Document**：来源材料。
- **Raw Document Version**：材料的有意义版本变化。
- **Evidence**：题目对应的证据片段、链接、观察时间和保存权限。
- **Question Mention**：题目在一份资料中的提及，用于资料收录频率。

### 岗位与面试上下文

- **Company**：公司。
- **Job**：公司招聘岗位和 JD。
- **Interview**：一段可区分的面试经历。
- **Interview Occurrence**：Question 在 Interview 中被报告出现。

### 训练

- **Training Session**：一次训练会话。
- **Training Attempt**：对某题的一次回答。
- **Assessment**：用户自评和 LLM 评价。
- **Follow-up**：本次训练生成的追问。

### 系统运行

- **Embedding**：Question 或 Original Wording 的向量及模型元数据。
- **Import Batch**：一批待审核的导入结果。
- **Task Run**：后台任务执行、重试和错误信息。
- **User Preference**：个人岗位、领域、训练偏好和预算设置。

首版不需要多用户 User 表；个人配置可以按单用户环境处理。

## 8. 关键数据规则

1. Question 可以没有 Source。
2. Original Wording 不覆盖标准 Question。
3. Question Mention 不等于 Interview Occurrence。
4. JD 技术要求不等于面试题。
5. LeetCode 练习不等于真实面试出现。
6. 模型生成题不等于真实面试原题。
7. Embedding 相似度不等于同题证明。
8. LLM 建议不等于正式分类、关系或证据。
9. 所有自动导入先进入 Inbox。
10. 合并必须可恢复，训练历史不能丢失。
11. 来源更新不能静默覆盖个人 Question。
12. 资料频率和面试报告频率分别查询和展示。

## 9. 主要工作流

### 9.1 手动录入 Workflow

```text
用户输入题目
→ 创建 Question
→ 进入待整理
→ 补充领域、标签、难度
→ 可选地关联来源和原始问法
→ 标记已整理
```

### 9.2 来源收录 Workflow

```text
发现来源
→ 判断是否允许获取和保存
→ Fetcher 获取或由用户提供合法摘录
→ Raw Document/Version
→ 规则或 LLM 提取候选题
→ 收录预览
→ 用户批量审核
→ 创建 Question、Original Wording、Evidence
```

### 9.3 去重 Workflow

```text
新题进入 Inbox
→ 字符串/别名/Embedding 召回候选
→ LLM 按同题标准判断
→ 用户确认 SAME/RELATED/DIFFERENT/UNCERTAIN
→ 保存关系或执行可恢复合并
```

### 9.4 检索 Workflow

```text
用户输入查询
→ 解析明确筛选条件
→ 严格条件过滤
→ 关键词与别名召回
→ Embedding 召回
→ 排名融合
→ 复杂歧义查询才 LLM 重排
→ 返回 Question、命中依据和证据
```

### 9.5 训练 Workflow

```text
创建训练会话
→ 程序选题
→ 用户回答
→ LLM 评价
→ 程序记录
→ 必要时有限追问
→ 进入下一题
→ 会话结束
```

## 10. 采集合规与质量要求

每个来源接入前检查：

- robots.txt
- Terms of Service
- 是否需要登录
- CAPTCHA 和访问限制
- 官方 API 或 RSS
- 频率限制
- 页面结构稳定性
- License 和内容来源
- 是否允许本地保存
- 是否允许发送给外部模型

任何来源都不默认允许批量复制内容。搜索引擎结果只能用于发现候选链接，不能替代原始页面证据。

## 11. 费用与模型治理

- 所有模型调用记录 Token 使用量和估算成本。
- 后台批量任务遵守月度预算上限。
- 15 元触发提醒，20 元暂停后台自动模型任务。
- 失败重试必须有次数和范围上限。
- 普通本地搜索不调用生成式 LLM。
- 复杂查询、去重判断和训练评价才调用 LLM。
- 任何模型输出都先经过结构校验和业务规则校验。
- 模型版本和价格不写死在业务逻辑中。

## 12. MVP 页面与验收标准

### 页面

1. 题库首页
2. 快速添加题目
3. Question 详情与编辑
4. 收录 Inbox
5. 搜索和筛选
6. 来源与证据查看
7. 训练页面
8. 训练历史
9. 任务和备份设置

### 核心验收场景

```text
创建一道没有来源的题
→ 后补原始问法和来源
→ 整理分类、标签、难度
→ 发现并确认重复关系
→ 按岗位、领域和语义找到它
→ 完成一次训练
→ 保存回答和评价
→ 备份并恢复题库
```

### MVP 不通过的情况

- 必须先有面经才能创建 Question
- 自动导入直接写入正式题库
- 资料收录次数被错误显示为面试次数
- LLM 可以未经程序校验直接修改题库
- 合并题目后丢失原始问法、来源或训练历史
- 备份无法恢复
- 普通关键词查询必须依赖 LLM 才能工作

## 13. 开发阶段计划

### Phase 0：工程骨架与题库基础

- 建立 Web 应用和本地运行方式
- 完成 Question、Original Wording、Topic、Tag 的基础能力
- 实现手动创建、编辑、列表和详情
- 实现本地备份和导出

### Phase 1：来源与收录预览

- Source、Raw Document、Evidence 和 Import Batch
- 手动 URL/摘录收录
- 精选 GitHub 来源适配
- 规则提取和收录预览
- 批量接受、忽略和修改

### Phase 2：归一化、去重和检索

- Original Wording 关联
- 题目关系
- Embedding 生成和本地相似度
- 关键词/别名与向量排名融合
- LLM Judge Workflow
- 30～50 条检索评估集

### Phase 3：基础个人训练

- Training Session 和 Attempt
- 选题规则
- 回答评价
- 有限追问
- 训练历史和简单薄弱点记录

### Phase 4：持续运行能力

- GitHub 和目标招聘官网定期检查
- 后台任务、重试和预算治理
- 来源版本变化建议
- 更完善的证据和市场分析查询

### Phase 5：可选增强

- 受控题库 Chatbot
- 更丰富的个人能力画像
- 多 Provider 备选
- 开放式联网搜索 Agent
- 远程访问或云端部署

## 14. 评估指标

### 数据质量

- 自动提取结果的忠实性
- 原始问法证据覆盖率
- 错误合并率
- 用户每批收录所需修订量
- 来源和时间证据完整率

### 检索质量

- 评估集 Recall@K
- Top 结果的人工有用率
- 关键词查询是否被正确命中
- 同义改写是否能召回
- 无关查询是否避免强行返回

### 训练质量

- 训练记录是否完整保存
- 评价是否能指出具体缺陷
- 追问是否与原题和回答相关
- 训练结果能否影响后续选题

### 运行质量

- 任务失败可重试
- 重复执行不会重复写入
- 备份可恢复
- 模型预算不超限
- 无模型时基础题库功能仍可使用

## 15. 风险与应对

| 风险 | 应对 |
|---|---|
| 来源条款变化 | 逐站核实，保留来源状态，必要时改为人工收录 |
| 页面结构变化 | 来源适配器隔离，失败进入任务日志，不污染题库 |
| 过度合并题目 | Embedding 仅召回，LLM 建议，用户确认，可恢复 |
| LLM 幻觉或越权 | 结构化输出、程序校验、Inbox 审核和证据边界 |
| 模型费用失控 | Token 记录、预算提醒、后台硬限额 |
| 数据丢失 | 自动版本化备份、手动备份、恢复验证和导出 |
| 分类体系膨胀 | 受控词表、别名管理、用户确认 |
| 自动化维护成本过高 | 精选来源、按来源适配、自动化失败降级人工 |
| SQLite 单写入限制 | 短事务、单实例、后台任务和 Web 进程协调写入 |

## 16. 交付边界

开发人员交付时应至少提供：

- 可本机启动的 Web 应用
- 数据初始化和迁移方式
- Question-first 录入闭环
- 收录预览和人工审核闭环
- 题目关系和可恢复合并
- 基础混合检索
- 基础训练 Workflow
- 备份、恢复和导出
- 模型调用、任务和错误日志
- README、运行说明和已知限制
- MVP 验收场景的测试记录

## 17. 最终架构结论

本项目首版采用：

> 模块化单体 Web 应用＋本地 SQLite＋独立后台 Worker＋精选来源适配器＋受控 LLM Workflow＋本地向量检索＋基础个人训练流程。

系统的稳定性来自传统程序、明确的数据语义、来源证据和持久化状态；LLM 用于需要语言理解的环节；Agent 只作为后续开放式能力的候选，不是首版前提。

---

## 18. 实施级需求规格

前面的章节是架构决策，本章将决策转换为可执行的产品和工程要求。开发人员可以据此拆分任务；如果实现过程中需要改变语义规则，必须先更新本文档并说明影响。

### 18.1 功能需求编号

#### Question 题库

- **FR-Q-001**：用户可以不填写来源，直接创建 Question。
- **FR-Q-002**：用户可以编辑标准题目、作答任务、领域、标签、参考难度和整理状态。
- **FR-Q-003**：用户可以为一个 Question 关联多个 Original Wording。
- **FR-Q-004**：用户可以后补 Source、Raw Document、Evidence、Company、Job 和 Interview。
- **FR-Q-005**：删除 Question 不能静默删除其原始问法、证据和训练历史；默认使用归档或软删除。
- **FR-Q-006**：Question 的整理状态首版只有 `INBOX` 和 `ORGANIZED`。
- **FR-Q-007**：创建方式至少区分 `USER_CREATED`、`MANUAL_IMPORT`、`WORKFLOW_IMPORT` 和 `MODEL_GENERATED`。
- **FR-Q-008**：模型生成的训练题不能自动成为真实来源题或 Interview Occurrence。

#### 原始问法与关系

- **FR-R-001**：Original Wording 必须保存原文和来源定位。
- **FR-R-002**：标准化文本不得覆盖 Original Wording。
- **FR-R-003**：系统必须支持 `SAME`、`RELATED`、`VARIANT`、`PREREQUISITE` 和 `UNCERTAIN` 等关系状态。
- **FR-R-004**：去重建议必须经过用户确认后才成为正式关系。
- **FR-R-005**：合并操作必须可恢复，且恢复后原始来源和训练记录仍可访问。

#### 来源与证据

- **FR-S-001**：每个 Source 记录来源类型、主机、规则检查状态和允许的获取方式。
- **FR-S-002**：Raw Document 可以只保存链接和元数据，也可以按权限保存全文或片段。
- **FR-S-003**：Raw Document 必须区分当前版本、历史版本和失效状态。
- **FR-S-004**：Evidence 至少包括来源 URL、观察时间、证据范围和保存权限。
- **FR-S-005**：同一材料重复检查不产生新的 Question Mention。
- **FR-S-006**：来源变化只能生成更新建议，不能自动覆盖 Question。
- **FR-S-007**：Question Mention 和 Interview Occurrence 必须使用不同的业务对象和统计口径。

#### 导入与审核

- **FR-I-001**：所有自动提取结果先进入 Import Inbox。
- **FR-I-002**：用户可以批量接受、忽略和修改候选题。
- **FR-I-003**：每个候选题必须显示原文证据和提取置信度。
- **FR-I-004**：导入任务必须支持部分成功；单个候选失败不能回滚整个批次。
- **FR-I-005**：重复运行同一来源任务不能重复创建相同 Raw Document Version 或 Question Mention。

#### 检索

- **FR-X-001**：关键词检索不依赖 LLM 即可工作。
- **FR-X-002**：支持岗位、领域、标签、难度、公司、时间和整理状态筛选。
- **FR-X-003**：支持标准 Question 和已确认 Original Wording 命中。
- **FR-X-004**：支持关键词/别名召回和 Embedding 语义召回。
- **FR-X-005**：使用排名融合，不直接混合未经校准的原始分数。
- **FR-X-006**：普通查询不调用生成式 LLM；复杂歧义查询可对少量候选重排。
- **FR-X-007**：结果必须显示命中依据，包括关键词、别名、向量、关系或筛选条件。
- **FR-X-008**：结果必须分别显示资料收录次数和面试报告次数。

#### 训练

- **FR-T-001**：用户可以按岗位、领域、难度和数量创建训练会话。
- **FR-T-002**：程序根据规则选择 Question，不允许 LLM 直接修改训练会话状态。
- **FR-T-003**：用户可以提交多次回答，每次回答都保留历史。
- **FR-T-004**：LLM 评价必须输出固定结构，包括优点、缺陷、覆盖点、建议和置信度。
- **FR-T-005**：追问数量有上限，追问必须关联当前 Question 和当前回答。
- **FR-T-006**：训练结束后保存 Session、Attempt、Assessment 和 Follow-up。
- **FR-T-007**：训练生成内容默认不进入正式题库和真实面试统计。

#### 任务、备份与设置

- **FR-O-001**：后台任务独立于浏览器页面运行。
- **FR-O-002**：任务支持状态、错误、重试次数、部分成功和人工重跑。
- **FR-O-003**：模型调用记录用途、Provider、模型版本、Token 用量和预算估算。
- **FR-O-004**：接近月度预算时提醒，超过后台硬上限时暂停自动模型任务。
- **FR-O-005**：支持自动备份、手动备份、恢复验证和数据导出。

### 18.2 非功能需求

- **NFR-001 可恢复性**：合并、归档、来源更新和导入都不能造成不可逆的数据丢失。
- **NFR-002 可解释性**：自动化结果必须能追溯到来源、规则、模型版本和命中依据。
- **NFR-003 可降级性**：没有 LLM 或 Embedding 时，手动题库和关键词搜索仍可用。
- **NFR-004 幂等性**：同一导入任务重复执行不会产生重复实体或重复统计。
- **NFR-005 成本控制**：模型调用必须可统计、可限制、可关闭后台自动调用。
- **NFR-006 本地优先**：首版默认不把整个题库上传到任何外部服务。
- **NFR-007 可迁移性**：题目、来源、证据、关系和训练历史可以导出。
- **NFR-008 可测试性**：领域规则、检索、状态流转和模型适配器可以独立测试。

---

## 19. 领域数据契约

以下不是特定 ORM 语法，而是开发必须满足的字段语义。字段名可以调整，但含义不能改变。

### 19.1 Question

必需语义：

- 稳定标识
- 标准题目文本
- 作答任务类型：解释、设计、实现、调试、比较等
- 整理状态：`INBOX` / `ORGANIZED`
- 创建方式
- 内容来源类型：自建、面经、JD、练习题、模型生成等
- 参考难度及难度来源
- 创建时间和更新时间

可选语义：

- 用户备注
- 参考答案或评价要点
- 训练提示
- 归档时间

### 19.2 Original Wording

- 原始问法文本
- 所属 Question
- 来源类型
- Raw Document 或 Evidence 引用
- 原文定位信息
- 提取方式：人工、规则、LLM
- 提取置信度
- 原始观察时间

### 19.3 Source / Raw Document / Evidence

Source：

- 来源名称和类型
- 主机或平台标识
- 获取策略：人工、API、RSS、HTTP、浏览器
- 合规检查状态
- 本地保存策略
- 外部模型发送策略

Raw Document：

- 来源 URL
- 标题和元数据
- 当前状态：有效、失效、待检查、禁止保存
- 当前版本引用
- 首次观察时间和最近检查时间
- 内容保存级别

Raw Document Version：

- 内容指纹
- 观察时间
- 版本变化说明
- 允许保存的正文或片段
- 抓取任务引用

Evidence：

- URL
- 片段或个人笔记
- 文档定位信息
- 观察时间
- 是否允许持久化
- 是否允许外发给 LLM

### 19.4 Company / Job / Interview

Company：公司名称及规范化名称。

Job：公司、岗位名称、岗位 URL、JD 观察时间和技术要求。JD 技术要求属于岗位上下文，不自动生成面试 Occurrence。

Interview：公司、Job、来源面经、面试日期或时间范围、轮次、面试者叙述可信度和备注。信息不足时可以只建立部分上下文。

Interview Occurrence：Question、Interview、报告证据、面试轮次、原始问法和置信度。

### 19.5 Topic / Tag

Topic 是受控知识目录，支持父子层级；Tag 是补充描述。正式 Topic/Tag 必须可追踪创建来源和确认人。模型建议不能绕过确认直接扩大正式词表。

### 19.6 Training

Training Session：岗位、领域、开始/结束时间、会话状态和选题规则快照。

Training Attempt：Question、展示文本、用户回答、开始/结束时间和是否完成。

Assessment：LLM Provider、模型版本、评价结构、用户修订和评价置信度。

Follow-up：当前 Attempt、追问文本、回答、追问轮次和是否完成。默认不创建 Question。

### 19.7 Embedding

- 目标类型：Question 或 Original Wording
- 目标标识
- Provider
- 模型 ID 和版本
- 向量维度
- 向量值
- 生成时间
- 文本指纹
- 生成状态和错误信息

同一个目标可以存在不同模型版本的向量，但检索时只能使用同一模型和维度的向量集合。

---

## 20. 状态机与状态转移

### 20.1 Question 整理状态

```text
INBOX --用户确认整理--> ORGANIZED
INBOX --用户忽略------> ARCHIVED
ORGANIZED --用户归档--> ARCHIVED
```

归档不是物理删除。恢复操作必须能够回到原状态。

### 20.2 Import Item

```text
DISCOVERED → FETCHED → EXTRACTED → REVIEW_PENDING
                                  ├→ ACCEPTED
                                  ├→ EDITED
                                  ├→ IGNORED
                                  └→ FAILED
```

### 20.3 Dedup Candidate

```text
DISCOVERED
  → LLM_REVIEWED
  → USER_CONFIRMED_SAME
  → USER_CONFIRMED_RELATED
  → USER_CONFIRMED_DIFFERENT
  → UNCERTAIN
```

### 20.4 Background Task

```text
PENDING → RUNNING → SUCCEEDED
                  ├→ PARTIAL_SUCCESS
                  ├→ FAILED_RETRYABLE
                  └→ FAILED_FINAL
```

### 20.5 Training Session

```text
READY → PRESENTED → ANSWERING → EVALUATING
                                  ├→ FOLLOW_UP
                                  └→ RECORDED
FOLLOW_UP → RECORDED → NEXT / COMPLETED
```

所有状态转移由程序执行，LLM 不能直接提交状态转移。

---

## 21. LLM 结构化契约

LLM 不直接拥有数据库写权限。每个 Workflow 由程序准备输入、调用模型、校验输出，再生成预览或执行结果。

### 21.1 题目提取输出

每个候选至少包含：

- 候选题目文本
- 作答任务类型
- 原始证据定位
- 相关上下文
- 领域建议
- 标签建议
- 难度建议及理由
- 提取置信度
- 是否明确来自原文

### 21.2 归一化输出

- 标准题目建议
- 保留的原始问法
- 与原文相比的改写说明
- 不能确定的部分
- 是否建议拆分子题

### 21.3 去重判断输出

```text
relation: SAME | RELATED_BUT_DIFFERENT | DIFFERENT | UNCERTAIN
reason: string
shared_goal: string
key_difference: string
confidence: number
```

`confidence` 只表示模型判断信心，不是事实概率，也不能替代用户确认。

### 21.4 分类输出

- 已存在 Topic/Tag 的 ID 或名称
- 新标签建议
- 每个建议的理由
- 置信度
- 是否需要人工确认

### 21.5 训练评价输出

- 回答覆盖的考查点
- 正确部分
- 错误或缺失部分
- 事实性风险
- 表达和结构建议
- 参考改进方向
- 总体评价
- 是否建议追问
- 追问文本
- 评价置信度

所有输出必须通过 JSON Schema/运行时校验；解析失败进入重试或人工处理，不允许猜测性写入。

### 21.6 Prompt 版本

每个 Workflow 的系统提示词、输出契约和规则都必须有版本标识。历史 Assessment、提取结果和去重判断要能追溯当时使用的 Prompt 版本。

---

## 22. 检索实现规格

### 22.1 查询处理顺序

1. 解析明确筛选条件。
2. 对结构化字段执行严格过滤。
3. 使用标题、标准题目、Original Wording、Topic 和别名执行关键词召回。
4. 对 Question 和确认关联的 Original Wording 进行向量召回。
5. 使用排名融合合并结果。
6. 精确标题和受控别名命中获得确定性优先。
7. 复杂歧义查询可把少量候选交给 LLM 重排。
8. 返回结果和命中依据。

### 22.2 初始可调参数

以下只是首轮实验起点，不是永久写死的业务事实：

- 候选召回上限：20～30
- 结果展示数量：约 10
- LLM 重排候选：8～12
- 去重候选可以使用更宽的召回范围
- 相似度最低阈值和排名融合常数由评估集校准

实现时应把参数配置化，并记录每次评测使用的参数。

### 22.3 结果解释

每条结果至少显示一种命中依据：

- 标题精确命中
- 关键词命中
- 别名命中
- Topic/Tag 命中
- 语义相似候选
- 已确认相关关系
- 公司、岗位或时间筛选

资料收录次数和 Interview Occurrence 次数必须分别计算和展示。

---

## 23. 采集适配器规格

每个来源适配器都实现相同的逻辑能力，但不强求相同的访问方式：

1. Discovery：发现候选 URL。
2. Permission Check：记录允许获取、保存和外发的策略。
3. Fetch：使用 API、RSS、HTTP、浏览器或人工输入。
4. Normalize：提取标题、作者、时间和来源元数据。
5. Change Detect：判断内容是否有实质变化。
6. Extract Preview：生成候选题和证据预览。
7. Persist：经过审核后写入正式题库。

适配器必须返回统一的任务结果：成功、部分成功、可重试失败、最终失败，并包含原因和来源 URL。

### GitHub

- 仅处理用户明确加入白名单的仓库。
- 优先使用官方 API。
- 保存仓库、路径、提交版本、License 和原始链接。
- 不因仓库公开就默认拥有任意复制和再分发权。
- 文件变更以提交或内容指纹判断。

### 牛客

- 首版人工发现和人工提供合法摘录。
- 不设计未经授权的批量搜索或抓取。
- 用户确认后才创建 Question Mention 或 Interview Occurrence。

### 招聘官网

- 逐站判断 robots、条款、API/RSS 和页面结构。
- 优先抓取目标公司和目标岗位。
- JD 变化生成更新建议，不自动重写历史 Job 或 Question。

### LeetCode

- 保存题号、标题、URL、标签、难度和个人练习状态。
- 不批量复制题目正文。
- 练习记录不创建 Interview Occurrence。

### 其他社区

- 以 URL、RSS 和人工摘录为主。
- 搜索引擎只用于发现页面，不作为最终证据。

---

## 24. API 与页面边界

以下是逻辑 API，不规定必须采用哪种 HTTP 风格；实现时可以映射为 Next.js Route Handler 或独立服务接口。

### Question

- `POST /questions`：创建 Question
- `GET /questions`：列表、关键词和筛选
- `GET /questions/:id`：详情、问法、证据、关系和训练摘要
- `PATCH /questions/:id`：编辑 Question
- `POST /questions/:id/archive`：归档
- `POST /questions/:id/restore`：恢复

### Sources and Imports

- `POST /sources`：登记来源
- `POST /imports/manual`：提交 URL 或合法摘录
- `POST /imports/github`：触发白名单仓库导入
- `GET /imports/:batchId`：查看导入批次
- `GET /inbox`：查看待审核候选
- `POST /inbox/:itemId/accept`：接受候选
- `POST /inbox/:itemId/ignore`：忽略候选
- `POST /inbox/:itemId/edit`：修改后接受

### Relationships and Evidence

- `POST /questions/:id/wordings`：关联原始问法
- `POST /questions/:id/relations`：确认关系
- `POST /questions/:id/evidence`：关联证据
- `POST /dedup/:candidateId/decision`：提交去重确认

### Search

- `GET /search/questions?q=...`：关键词/语义查询
- `POST /search/questions`：复杂查询和结构化筛选

### Training

- `POST /training/sessions`：创建训练会话
- `GET /training/sessions/:id`：会话状态
- `POST /training/sessions/:id/answer`：提交回答
- `POST /training/sessions/:id/follow-up-answer`：提交追问回答
- `POST /training/sessions/:id/finish`：结束会话

### Operations

- `GET /tasks`：任务状态
- `POST /tasks/:id/retry`：重试任务
- `POST /backups`：手动备份
- `POST /backups/:id/verify`：验证恢复
- `GET /exports/questions`：导出题库和训练数据

所有写入接口必须经过领域服务校验；页面不能直接拼接数据库写操作。

---

## 25. 后台任务设计

### 25.1 任务类型

- `SOURCE_DISCOVERY`
- `SOURCE_FETCH`
- `RAW_DOCUMENT_CHANGE_CHECK`
- `QUESTION_EXTRACTION`
- `QUESTION_NORMALIZATION`
- `DEDUP_CANDIDATE_DISCOVERY`
- `LLM_DEDUP_JUDGE`
- `CLASSIFICATION_SUGGESTION`
- `EMBEDDING_GENERATION`
- `BACKUP`
- `BACKUP_VERIFY`

### 25.2 幂等规则

- Source URL 使用规范化 URL 和来源版本指纹去重。
- Raw Document Version 使用内容指纹判断是否为新版本。
- Question Mention 使用材料版本、Question 和证据定位去重。
- Embedding 使用目标文本指纹、Provider、模型版本和维度去重。
- LLM Workflow 失败重试不能创建重复 Question。

### 25.3 重试规则

- 网络临时错误：有限次数指数退避。
- CAPTCHA、登录、权限和条款不允许：不重试，转人工。
- 模型结构解析错误：有限次重试，仍失败则进入人工审核。
- 数据库冲突：重新读取状态后决定跳过或重试。
- 每次任务保存失败原因和最后一次响应摘要，避免保存敏感密钥。

---

## 26. 开发任务拆分

### Sprint 1：基础工程和 Question

- 建立 Next.js/React/TypeScript Web 应用。
- 建立 Node.js 应用服务层和本地 Worker 入口。
- 建立 SQLite 数据访问和迁移机制。
- 实现 Question、Topic、Tag、Original Wording 基础领域服务。
- 实现快速创建、编辑、列表和详情页面。
- 编写 Question 领域单元测试。

### Sprint 2：来源、证据和 Inbox

- 实现 Source、Raw Document、Version、Evidence。
- 实现手动 URL/摘录收录。
- 实现 Import Batch、Import Item 和 Inbox 页面。
- 实现批量接受、忽略和修改。
- 实现内容指纹和重复导入保护。

### Sprint 3：精选 GitHub 导入

- 实现仓库白名单。
- 实现 API 获取、提交版本记录和 License 元数据。
- 实现来源更新检查。
- 实现失败、限流和部分成功显示。
- 只接入一个经过核实的测试仓库，再扩展。

### Sprint 4：LLM 抽取和归一化

- 建立生成式 LLM Provider 接口。
- 实现结构化输出校验。
- 实现题目提取、归一化、分类和难度建议 Workflow。
- 保存 Prompt 版本、模型版本和调用成本。
- 所有结果进入 Inbox。

### Sprint 5：去重和关系

- 实现字符串/别名候选。
- 实现 Embedding 生成和本地相似度召回。
- 实现 LLM Judge。
- 实现用户确认、关系保存和可恢复合并。
- 编写同题、相关题和不同题测试集。

### Sprint 6：混合检索

- 实现严格筛选。
- 实现关键词/别名检索。
- 实现向量检索和排名融合。
- 实现命中依据展示。
- 建立 30～50 条检索评估集。
- 仅在复杂查询中接入 LLM 重排。

### Sprint 7：训练 Workflow

- 实现 Training Session、Attempt、Assessment、Follow-up。
- 实现训练状态机。
- 实现按岗位、领域、难度和历史记录选题。
- 实现 LLM 评价和有限追问。
- 实现训练历史页面。

### Sprint 8：运行保障

- 实现持久化调度规则和 Worker 周期检查。
- 实现任务错误、重试和预算控制。
- 实现自动备份、手动备份和恢复验证。
- 实现数据导出。
- 完成端到端验收。

---

## 27. 测试计划

### 27.1 领域测试

- 无来源创建 Question。
- 一个 Question 关联多个 Original Wording。
- 原始问法不被标准题覆盖。
- 大题和子题关系正确。
- 归档和恢复不丢数据。
- 合并和撤销合并不丢来源和训练历史。

### 27.2 证据与统计测试

- JD 要求不会生成 Interview Occurrence。
- LeetCode 练习不会生成 Interview Occurrence。
- GitHub 资料出现不会自动生成 Interview Occurrence。
- 同一材料重复抓取不增加 Mention 次数。
- 不同 Interview 的 Occurrence 可以分别统计。
- 证据不足的公司、岗位和时间查询不进入严格计数。

### 27.3 检索测试

- 精确标题优先。
- 受控别名可以召回。
- 同义改写可以召回。
- 完全无关查询不会因为 Top K 被强行标记为相关。
- 关键词检索在 Embedding/LLM 不可用时仍可用。
- LLM 重排失败时返回本地排序结果。

### 27.4 Workflow 测试

- 模型输出不符合契约时不会写入正式题库。
- 重试不会重复创建实体。
- 部分失败保留已成功结果。
- 预算达到硬上限后后台模型任务停止。
- 任务重启后可以从持久状态继续或安全跳过。

### 27.5 训练测试

- 会话状态只能按规定路径转换。
- 多次回答历史不互相覆盖。
- 追问与当前 Question 关联。
- 训练生成内容不进入真实来源统计。
- LLM 不可用时可以查看已有题目和训练历史。

### 27.6 备份测试

- 自动备份按计划生成。
- 手动备份可生成独立版本。
- 备份恢复后 Question、Evidence、Relation 和 Training Attempt 数量一致。
- 恢复不会覆盖当前数据，除非用户明确选择目标。

---

## 28. 开发人员实施注意事项

1. 不要把 LLM 输出当作数据库事实；所有输出先是建议或预览。
2. 不要把 Embedding 分数当作同题证明。
3. 不要把资料收录次数显示为面试次数。
4. 不要要求 Question 必须先拥有来源。
5. 不要让来源更新静默改写个人题目。
6. 不要为了统一而把牛客、GitHub、招聘官网强行使用同一种采集方式。
7. 不要让模型直接访问整个数据库；通过受限查询工具提供必要候选。
8. 不要把浏览器自动化实现成绕过验证或登录限制的机制。
9. 不要在首版引入未被需求证明必要的基础设施。
10. 如果需求、语义或统计口径出现歧义，优先暂停写入并进入 Inbox 或人工确认。

## 29. 实施启动条件

开发可以开始的前提：

- 本计划书版本固定并进入版本控制。
- 开发人员确认 D0～D17 的语义约束。
- 明确第一个可测试的来源和测试数据。
- 准备 30～50 条检索评估样本。
- 准备一组题目去重样本。
- 准备模型 API 的预算和数据外发配置。
- 明确本机数据目录和备份目录。

第一轮实现只做 Sprint 1，不同时实现全量采集、复杂 Agent 和高级推荐。
